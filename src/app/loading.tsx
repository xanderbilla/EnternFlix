export default function Loading() {
  return (
    <div
      className="min-h-screen w-full bg-zinc-900"
      role="status"
      aria-live="polite"
      aria-label="Loading content"
    />
  );
}
